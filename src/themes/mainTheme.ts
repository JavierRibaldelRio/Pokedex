// The Main theme of the Pokedex, Red & and White. Like Pokeballs :)

import { createTheme } from "@mui/material";
import { red } from '@mui/material/colors';

const mainTheme = createTheme({
    palette: {
        primary: red,

    }
})

export default mainTheme;